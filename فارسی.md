# CFnew - ترمینال v2.9.3

**زبان:** [中文](README.md) | [فارسی](فارسی.md)

[گروه تلگرام](https://t.me/+ft-zI76oovgwNmRh)

## 📖 فهرست مطالب

1.  [معرفی و امکانات](#معرفی-و-امکانات)
2.  [معرفی قابلیت‌ها (Capabilities Showcase)](#معرفی-قابلیت‌ها-capabilities-showcase)
3.  [مفاهیم کلیدی (به زبان ساده)](#مفاهیم-کلیدی-به-زبان-ساده)
4.  [تحلیل عمیق پروتکل‌های "فقط لینک" (Link-Only)](#تحلیل-عمیق-پروتکل‌های-فقط-لینک-link-only)
5.  [دانشنامه پیکربندی (Configuration Encyclopedia)](#دانشنامه-پیکربندی-configuration-encyclopedia)
    *   [احراز هویت پایه](#۱-احراز-هویت-پایه-identity)
    *   [رله و مخفی‌سازی](#۲-رله-و-مخفی‌سازی-relay--stealth)
    *   [تنظیم پروتکل‌ها](#۳-تنظیم-پروتکل‌ها-protocols)
    *   [کنترل پیشرفته](#۴-کنترل-پیشرفته-و-بهینه‌سازی-advanced--preferred)
6.  [راهنمای پیکربندی نهایی: فعال‌سازی همه چیز](#راهنمای-پیکربندی-نهایی-فعال‌سازی-همه-چیز)
7.  [راهنمای نصب از صفر (Zero to Hero)](#راهنمای-نصب-از-صفر-zero-to-hero)
8.  [تحلیل عمیق ProxyIP](#تحلیل-عمیق-proxyip)
9.  [راهنمای تنظیمات کلاینت](#راهنمای-تنظیمات-کلاینت)
10. [عیب‌یابی (Troubleshooting)](#عیب‌یابی-troubleshooting)
11. [مدیریت API](#مدیریت-api)
12. [تاریخچه ستاره](#تاریخچه-ستاره)

---

## معرفی و امکانات

CFnew یک اسکریپت پروکسی سبک است که بر روی Cloudflare Workers اجرا می‌شود. این ابزار از شبکه جهانی Cloudflare استفاده می‌کند تا اتصالی سریع و ایمن برای شما فراهم کند.

*   **پشتیبانی همزمان پروتکل‌ها**: پشتیبانی از پروتکل‌های بومی VLESS، Trojan، xhttp و پروتکل‌های رله‌مانند VMess و Shadowsocks.
*   **پنهان‌سازی بالا**: پشتیبانی از مسیر سفارشی (Custom Path) برای شبیه‌سازی یک وب‌سایت معمولی و جلوگیری از شناسایی پنل.
*   **مدیریت گرافیکی**: ذخیره تنظیمات در KV، امکان تغییر آنی تنظیمات بدون نیاز به کدنویسی مجدد.
*   **ابزارهای داخلی**: تست تاخیر (Ping)، مدیریت IPهای تمیز، و پروکسی DoH (DNS-over-HTTPS).
*   **اشتراک هوشمند**: تشخیص خودکار نرم‌افزار شما (Clash, v2rayNG و...) و ارائه فرمت مناسب.

---

## معرفی قابلیت‌ها (Capabilities Showcase)

سی‌اف‌نیو (CFnew) فقط یک اسکریپت پروکسی ساده نیست، بلکه مجموعه‌ای از ابزارهای قدرتمند است:

*   **🛡️ قدرت پروکسی (Proxy Power)**:
    *   **بومی (Native)**: پشتیبانی از VLESS, Trojan, VLESS gRPC با بهترین عملکرد.
    *   **رله (Relay)**: پشتیبانی از VMess و Shadowsocks برای سازگاری با دستگاه‌های قدیمی.
*   **📡 مدیریت اشتراک (Subscription Manager)**:
    *   تشخیص هوشمند کلاینت‌های Clash, v2rayNG, Sing-box, Surge, Quantumult X.
    *   تولید خودکار کانفیگ متناسب با هر کلاینت بدون نیاز به تبدیل دستی.
*   **⚡ تست تاخیر (Latency Test)**:
    *   رابط گرافیکی داخلی برای تست پینگ HTTP تمام IPهای کلودفلر.
    *   افزودن سریع‌ترین IP به لیست ترجیحی با یک کلیک.
*   **🔒 رله DoH (DNS امن)**:
    *   ارائه رابط `/dns-query` با پشتیبانی از پروتکل استاندارد DoH.
    *   توزیع بار خودکار بین Google, Cloudflare, Quad9 برای جلوگیری از آلودگی DNS.
*   **⚙️ موتور API (API Engine)**:
    *   ارائه API نوع RESTful برای به‌روزرسانی خودکار IPهای تمیز.
    *   مناسب برای کاربران حرفه‌ای جهت نوشتن اسکریپت‌های زمان‌بندی شده.

---

## مفاهیم کلیدی (به زبان ساده)

برای درک بهتر عملکرد Worker، بیایید مفاهیم را ساده کنیم.

### تشریح یک درخواست شبکه (مثال پستچی)

تصور کنید می‌خواهید نامه‌ای برای یوتیوب بفرستید، اما اداره پست (فیلترچی) نامه‌های یوتیوب را مسدود می‌کند.

1.  **بسته‌بندی (Encapsulation)**: شما نامه یوتیوب را داخل یک پاکت بزرگتر می‌گذارید که روی آن نوشته شده «به Cloudflare». این کارِ **پروتکل VLESS/Trojan** است.
2.  **ارسال (Transmission)**: پستچی (فیلترچی) می‌بیند که نامه برای شرکت معتبر Cloudflare است، پس اجازه عبور می‌دهد.
3.  **باز کردن (Processing)**: Worker (در سرور Cloudflare) نامه را باز می‌کند و می‌فهمد مقصد اصلی یوتیوب است.
4.  **فوروارد (Relay)**:
    *   **حالت مستقیم**: Worker نامه را مستقیماً به یوتیوب می‌دهد.
    *   **حالت ProxyIP**: Worker برای امنیت بیشتر، نامه را به یک واسط دیگر (ProxyIP) می‌دهد تا او آن را به یوتیوب برساند.
5.  **پاسخ**: یوتیوب پاسخ را می‌دهد و مسیر برعکس طی می‌شود.

---

## تحلیل عمیق پروتکل‌های "فقط لینک" (Link-Only)

**چرا VMess, Shadowsocks, TUIC, Hysteria 2 به عنوان "فقط لینک" شناخته می‌شوند؟**

Cloudflare Workers در محیط خاصی اجرا می‌شوند که محدودیت‌های کلیدی دارد:
1.  **محدودیت UDP**: ورکرها در حال حاضر پشتیبانی محدودی از UDP دارند و نمی‌توانند مانند یک سرور استاندارد، هندشیک‌های پیچیده UDP را مدیریت کنند (این موضوع اجرای سرور Hysteria 2 و TUIC را غیرممکن می‌کند).
2.  **شنود پورت**: ورکرها فقط توسط کلودفلر اجرا می‌شوند و نمی‌توانند روی پورت‌های دلخواه (مثل ۱۰۸۰ یا غیره) گوش دهند.

**نقش Worker**:
در این سناریو، Worker نقش **"تولیدکننده لینک"** و **"مدیریت اشتراک"** را بازی می‌کند.
*   ورکر سرور Hysteria 2 یا TUIC را **اجرا نمی‌کند**.
*   ورکر فقط لینک‌هایی می‌سازد که به سرور شخصی شما اشاره دارند.

**نحوه استفاده**:
1.  **نیاز به سرور بک‌اند**: شما باید حتماً یک سرور جداگانه (VPS, Serv00 و غیره) داشته باشید که سرویس‌های Hysteria 2 / TUIC / VMess را اجرا کند.
2.  **توزیع کانفیگ**: از لینک اشتراک Worker برای رساندن تنظیمات به کلاینت‌های خود استفاده کنید.
3.  **مسیر ترافیک**:
    *   **TUIC/Hysteria**: کلاینت -> سرور شما (Worker فقط نقش دفترچه تلفن را دارد).
    *   **VMess/SS (حالت رله)**: کلاینت -> Worker -> سرور شما (از طریق متغیر `p` یا `s`).

---

## دانشنامه پیکربندی (Configuration Encyclopedia)

در اینجا تمام متغیرهای تنظیمات را به تفصیل توضیح می‌دهیم.

**اولویت KV در برابر متغیرهای محیطی**:
*   **اولویت با KV است**: اگر یک متغیر را هم در تنظیمات Worker (Environment Variables) و هم در پنل گرافیکی (KV) تنظیم کنید، **مقدار KV برنده است**.
*   **توصیه**: از متغیرهای محیطی برای تنظیمات اولیه (مثل UUID) و از پنل گرافیکی برای مدیریت روزمره استفاده کنید.

### ۱. احراز هویت پایه (Identity)

<div dir="rtl">

| متغیر | نام کامل | نوع | پیش‌فرض | توضیحات دقیق | چرا استفاده کنیم؟ | مثال |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **`u`** | **UUID** | رشته | (الزامی) | شناسه کاربری. قلب سیستم احراز هویت VLESS/Trojan. اگر این مقدار اشتباه باشد، اتصال رد می‌شود. | **امنیت**. جلوگیری از استفاده دیگران از ترافیک شما. | `84439981-04b6...` |

</div>

**نکته: چگونه UUID بسازیم؟**
می‌توانید از سایت‌های آنلاین "UUID Generator" استفاده کنید یا در ویندوز دستور `[guid]::NewGuid()` را در پاورشل اجرا کنید.

**درباره نسخه‌های UUID (v1 در برابر v4 و v7):**
*   **نسخه ۴ (v4 - تصادفی)**: **پیشنهاد ما**. کاملاً تصادفی ساخته می‌شود و بهترین حریم خصوصی را دارد. استانداردترین گزینه است.
*   **نسخه ۱ (v1 - زمان + مک‌لپ‌تاپ)**: شامل زمان ساخت و آدرس سخت‌افزاری (MAC) دستگاه شماست. اگرچه یکتاست، اما ممکن است اطلاعات دستگاه شما را لو دهد. برای پروکسی عمومی پیشنهاد نمی‌شود.
*   **نسخه ۷ (v7 - زمانی مرتب شده)**: استاندارد جدید که بر اساس زمان مرتب می‌شود و برای دیتابیس‌ها عالی است. برای استفاده در پروکسی تفاوت خاصی با v4 ندارد و قابل استفاده است.

### ۲. رله و مخفی‌سازی (Relay & Stealth)

<div dir="rtl">

| متغیر | نام کامل | نوع | پیش‌فرض | توضیحات دقیق | چرا استفاده کنیم؟ | مثال |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **`p`** | **ProxyIP** | رشته | (خالی) | آدرس سرور واسط. Worker درخواست را اول به اینجا می‌فرستد. | **رفع تحریم/مخفی‌سازی**. برای سایت‌هایی که IP کلودفلر را بسته‌اند یا برای مخفی ماندن IP اصلی Worker. | `1.2.3.4` یا `proxy.com` |
| **`s`** | **SOCKS5** | رشته | (خالی) | آدرس پروکسی ساکس ۵. اولویت آن از `p` بالاتر است. | **خروجی خاص**. اگر نیاز دارید IP نهایی شما متعلق به کشور خاصی باشد. | `user:pass@1.1.1.1:1080` |
| **`d`** | **Directory** | رشته | (خالی) | مسیر دسترسی به پنل. با تنظیم آن، مسیر پیش‌فرض UUID غیرفعال می‌شود. | **ضد اسکن**. پنل شما را از دید ربات‌ها مخفی می‌کند. | `/my-secret-path` |
| **`wk`** | **Worker Region** | رشته | (خودکار) | کد منطقه اجباری برای Worker. | **کاهش پینگ**. اتصال Worker را به نودهای نزدیک (مثل SG برای ایران) مجبور می‌کند. | `SG`, `DE` |

</div>

**نکات پیشرفته ProxyIP و SOCKS5**:
*   **`p` (ProxyIP)**: فقط از فوروارد TCP پشتیبانی می‌کند. اگر دامنه وارد کنید، Worker آن را Resolve می‌کند.
*   **`s` (SOCKS5)**: از فرمت `user:pass@host:port` استفاده کنید. اگر در پسورد کاراکتر خاص دارید، باید URL-Encode شود.

### ۳. تنظیم پروتکل‌ها (Protocols)

مقادیر: `yes` (روشن) یا `no` (خاموش).

<div dir="rtl">

| متغیر | پروتکل | پیش‌فرض | توضیحات |
| :--- | :--- | :--- | :--- |
| **`ev`** | **VLESS** | `yes` | **پردازش بومی**. سریع‌ترین و سبک‌ترین گزینه. |
| **`et`** | **Trojan** | `no` | **پردازش بومی**. ترافیک شبیه HTTPS واقعی است. |
| **`ex`** | **xhttp** | `no` | **پردازش بومی**. مبتنی بر HTTP POST. نیاز به تنظیم gRPC دارد. |
| **`evm`** | **VMess** | `no` | **فقط لینک**. Worker فقط لینک را می‌سازد. نیاز به بک‌اند دارد. |
| **`ess`** | **Shadowsocks** | `no` | **فقط لینک**. مشابه VMess. نیاز به بک‌اند دارد. |
| **`etu`** | **TUIC** | `no` | **فقط لینک**. Worker قادر به پردازش UDP نیست. نیاز به سرور مستقل. |
| **`ehy`** | **Hysteria 2** | `no` | **فقط لینک**. UDP. نیاز به سرور مستقل. |
| **`eg`** | **VLESS gRPC** | `no` | **پردازش بومی**. انتقال از طریق gRPC. |
| **`tp`** | **Trojan Pass** | (UUID) | رمز عبور Trojan. اگر خالی باشد از `u` استفاده می‌شود. |
| **`ech`** | **ECH** | `no` | **ویژگی پیشرفته**. رمزگذاری SNI. به طور خودکار `dkby=yes` را فعال می‌کند. |

</div>

**تحلیل فنی: VLESS در برابر Trojan**
*   **VLESS (بومی)**: یک پروتکل انتقال سبک و بدون وضعیت (Stateless). این پروتکل امضای دیجیتال یا هندشیک پیچیده ندارد و کاملاً به لایه انتقال (مثل TLS) متکی است.
    *   *مزیت*: کمترین تاخیر در اتصال، مصرف منابع بسیار کم.
*   **Trojan (بومی)**: طراحی شده تا ترافیک را دقیقاً شبیه به HTTPS معمولی نشان دهد. در هنگام اتصال، رمز عبور (که با SHA224 هش شده) را بررسی می‌کند.
    *   *مزیت*: در شبکه‌هایی با فیلترینگ شدید (DPI)، رفتار آن بسیار شبیه به وب‌گردی عادی است و مقاومت خوبی دارد.

### ۴. کنترل پیشرفته و بهینه‌سازی (Advanced & Preferred)

<div dir="rtl">

| متغیر | نام | پیش‌فرض | کاربرد |
| :--- | :--- | :--- | :--- |
| **`yx`** | **IP ترجیحی** | (خالی) | **اولویت اول**. لیست دستی IPها. فرمت: `IP:Port#Name`. |
| **`yxURL`** | **URL لیست** | (پیش‌فرض) | دریافت لیست IPها از یک فایل متنی آنلاین. |
| **`ipv4`** | **منابع IPv4** | `yes` | دریافت IPهای IPv4 از منبع آنلاین. |
| **`ipv6`** | **منابع IPv6** | `yes` | دریافت IPهای IPv6 از منبع آنلاین. |
| **`ispMobile`**| **همراه اول** | `yes` | دریافت IPهای مناسب همراه اول. |
| **`ispTelecom`**| **مخابرات** | `yes` | دریافت IPهای مناسب مخابرات. |
| **`ispUnicom`**| **ایرانسل/رایتل**| `yes` | دریافت IPهای مناسب ایرانسل و رایتل (لینک‌ها). |
| **`scu`** | **مبدل** | (پیش‌فرض) | آدرس سرویس تبدیل فرمت اشتراک (Sub-Converter). |
| **`homepage`**| **صفحه اصلی** | (خالی) | آدرسی که در ریشه دامنه `/` نمایش داده می‌شود (استتار). |
| **`epd`** | **دامنه ترجیحی** | `yes` | فعال‌سازی دامنه‌های بهینه داخلی. |
| **`epi`** | **IP ترجیحی** | `yes` | فعال‌سازی IPهای بهینه پویا. |
| **`qj`** | **کاهش سطح** | `yes` | **نکته**: برای فعال‌سازی باید `no` باشد! (منطق معکوس: Fallback=no یعنی غیرفعال). |
| **`dkby`** | **فیلتر پورت** | `no` | اگر `yes` باشد، فقط پورت‌های امن (TLS) در اشتراک قرار می‌گیرند. |
| **`rm`** | **تطبیق منطقه** | `yes` | اگر `no` باشد، تطبیق هوشمند منطقه غیرفعال می‌شود. |
| **`ae`** | **مدیریت API** | `no` | اگر `yes` باشد، API فعال می‌شود. |
| **`customDNS`** | **DNS سفارشی** | (پیش‌فرض) | **مخصوص KV**. آدرس DoH برای دریافت تنظیمات ECH. |
| **`customECHDomain`** | **دامنه ECH** | (پیش‌فرض) | **مخصوص KV**. دامنه‌ای که تنظیمات ECH از آن استعلام می‌شود. |

</div>

---

## راهنمای پیکربندی نهایی: فعال‌سازی همه چیز

اگر می‌خواهید از حداکثر توانایی اسکریپت استفاده کنید:

### اهداف
1.  داشتن **VLESS** و **Trojan** به عنوان پشتیبان.
2.  فعال‌سازی **ECH** برای عبور از سخت‌ترین فیلترها.
3.  استفاده از **ProxyIP** برای باز کردن یوتیوب/نتفلیکس.
4.  تغییر مسیر پنل برای **امنیت**.
5.  فعال‌سازی **API** برای به‌روزرسانی خودکار.

### مراحل (در تنظیمات KV)

1.  Worker را مستقر کرده و KV را متصل کنید.
2.  به پنل بروید و مقادیر زیر را تنظیم و ذخیره کنید:

*   **`ev`**: `yes`
*   **`et`**: `yes`
*   **`ech`**: `yes`
*   **`tp`**: `MyStrongPass123` (یک رمز ساده و قوی)
*   **`p`**: `proxyip.fxxk.dedyn.io` (یا هر ProxyIP سالم دیگر)
*   **`d`**: `/my-admin`
*   **`ae`**: `yes`
*   **`qj`**: `no` (فعال‌سازی سیستم فال‌بک هوشمند)
*   **`homepage`**: `https://www.aparat.com` (برای استتار به عنوان سایت آپارات)

### نتیجه
*   لینک پنل شما: `https://domain.com/my-admin`
*   تمام اتصالات رمزگذاری شده و امن هستند.
*   ترافیک از طریق ProxyIP عبور می‌کند و IP شما مخفی است.

---

## راهنمای نصب از صفر (Zero to Hero)

فقط ۵ دقیقه زمان می‌برد.

### مرحله ۱: آمادگی
1.  یک اکانت [Cloudflare](https://dash.cloudflare.com/sign-up) بسازید.
2.  یک UUID بسازید (می‌توانید از سایت‌های آنلاین تولید UUID استفاده کنید).

### مرحله ۲: ساخت Worker
1.  وارد پنل Cloudflare شوید. از منوی سمت چپ، **"Workers & Pages"** را انتخاب کنید.
2.  روی **"Create Application"** و سپس **"Create Worker"** کلیک کنید.
3.  یک نام انتخاب کنید (مثلاً `my-proxy`) و **"Deploy"** را بزنید.
4.  روی **"Edit code"** کلیک کنید.
5.  کدهای فایل `_worker.js` (یا `明文源吗`) این پروژه را کپی کرده و جایگزین تمام کدهای موجود در ادیتور کنید.
6.  دکمه **"Save and deploy"** را در بالا سمت راست بزنید.

### مرحله ۳: تنظیم KV (بسیار مهم)
*برای داشتن پنل گرافیکی، این مرحله الزامی است.*
1.  در پنل Cloudflare، دوباره به **"Workers & Pages"** و سپس زیرمنوی **"KV"** بروید.
2.  روی **"Create a namespace"** کلیک کنید. نامی مثل `CONFIG` بگذارید و **"Add"** را بزنید.
3.  به صفحه تنظیمات Worker خود برگردید. تب **"Settings"** و سپس **"Variables"**.
4.  پایین بروید تا به **"KV Namespace Bindings"** برسید.
5.  روی **"Add binding"** کلیک کنید.
    *   **Variable name**: حرف `C` را وارد کنید (باید C بزرگ باشد).
    *   **KV Namespace**: همان `CONFIG` که ساختید را انتخاب کنید.
6.  دکمه **"Save and deploy"** را بزنید.

### مرحله ۴: تنظیم متغیرها
1.  در همان صفحه **"Settings"** -> **"Variables"**، بخش **"Environment Variables"**.
2.  روی **"Add variable"** کلیک کنید.
    *   **Variable name**: `u`
    *   **Value**: آن UUID که در مرحله ۱ ساختید را وارد کنید.
3.  (اختیاری) یک متغیر دیگر با نام `p` بسازید و آدرس یک ProxyIP را در آن وارد کنید.
4.  **"Save and deploy"** را بزنید.

### مرحله ۵: شروع استفاده
به آدرس `https://your-worker-domain/YOUR-UUID` بروید. پنل مدیریت برای شما باز می‌شود!

### مرحله ۶: بررسی و اتصال
1.  در پنل، روی دکمه کپی لینک اشتراک کلیک کنید.
2.  لینک را در نرم‌افزار خود (v2rayNG, Clash, Shadowrocket) وارد (Import) کنید.
3.  اتصال را تست کنید (تست واقعی، نه فقط پینگ). اگر وصل شد، تبریک می‌گوییم!

---

## تحلیل عمیق ProxyIP

**عملکرد**:
در حالت عادی، وقتی Worker سایتی را باز می‌کند، سایت مقصد IP شرکت Cloudflare را می‌بیند. ProxyIP مثل یک واسط عمل می‌کند تا درخواست از طرف آن ارسال شود.

**چگونه یک ProxyIP سالم پیدا کنیم؟ (راهنمای سریع)**
1.  **دامنه‌های عمومی**: جامعه کاربری برخی دامنه‌های همیشه فعال مثل `proxyip.fxxk.dedyn.io` را نگهداری می‌کند.
2.  **سرور شخصی**: می‌توانید روی یک VPS ارزان که پورت‌های غیر ۴۴۳ دارد، سرویس VLESS/Trojan راه بیندازید و آدرس آن را در `p` وارد کنید.
3.  **اسکنر**: استفاده از ابزارهای اسکن IP کلودفلر برای یافتن IPهایی که پورت‌های غیرستاندارد (مثل ۲۰۵۳, ۲۰۸۳) را باز گذاشته‌اند.

---

## راهنمای تنظیمات کلاینت

برای بهترین نتیجه، تنظیمات زیر را در گوشی یا کامپیوتر خود اعمال کنید:

### v2rayNG (اندروید)
*   **Mux (چندپخشی)**: ترجیحاً **خاموش** باشد. در شبکه‌های ایران گاهی باعث اختلال می‌شود.
*   **Allow Insecure (گواهی نامعتبر)**: اگر دامنه شخصی ندارید و از `workers.dev` استفاده می‌کنید، ممکن است لازم باشد این گزینه را `true` کنید.
*   **Sniffing**: حتماً **روشن** باشد تا ترافیک سایت‌های داخلی و خارجی درست تفکیک شود.

### Shadowrocket (آیفون/iOS)
*   **Allow Insecure**: در تنظیمات کانفیگ فعال شود.
*   **آپدیت اشتراک**: گزینه "Update on Open" را روشن کنید تا همیشه IPهای سالم داشته باشید.

### Clash Meta / Mihomo
*   **هسته (Kernel)**: حتماً از هسته **Meta** استفاده کنید. هسته معمولی Premium ممکن است با ویژگی‌های جدید مثل Reality یا ECH مشکل داشته باشد.
*   **Fingerprint**: روی `chrome` تنظیم شود.

---

## عیب‌یابی (Troubleshooting)

**سناریو ۱: صفحه اشتراک باز می‌شود، اما اتصال قطع است (Timeout)**
*   **علت الف**: اینترنت شما دامنه‌های `workers.dev` را فیلتر کرده است.
    *   *راه حل*: یک دامنه شخصی (Custom Domain) به Worker متصل کنید یا از IP تمیز (Scanner) استفاده کنید.
*   **علت ب**: پورت مسدود است.
    *   *راه حل*: از پورت‌های دیگر (مثل 2053, 8443) استفاده کنید.
*   **علت ج**: UUID اشتباه است.
    *   *راه حل*: UUID کلاینت و سرور باید دقیقاً یکسان باشند.

**سناریو ۲: خطای "Error 1101: Worker Threw Exception"**
*   **علت**: کد Worker به مشکل خورده است.
*   *راه حل*: معمولاً یعنی KV متصل نشده است. چک کنید متغیر `C` به درستی به فضای نام KV متصل شده باشد.

**سناریو ۳: خطای "Error 502: Bad Gateway"**
*   **علت**: Worker نمی‌تواند به مقصد (یا ProxyIP) وصل شود.
*   *راه حل*: احتمالا ProxyIP شما (`p`) از کار افتاده است. آن را عوض کنید یا موقتاً پاک کنید.

**کدهای خطای رایج**:
*   **522 Connection Timed Out**: ورکر نمی‌تواند به مقصد (ProxyIP) وصل شود. احتمالا IP فیلتر شده است.
*   **523 Origin Unreachable**: مسیر به سرور مقصد مسدود است. پورت‌ها را چک کنید.
*   **1000 DNS Error**: تنظیمات DNS اشتباه است یا یک چرخه (Loop) ایجاد شده (ProxyIP دوباره به خود کلودفلر اشاره می‌کند).

**تکنیک‌های دیباگ**:
*   کنسول مرورگر را باز کنید (F12 -> Console) تا خطای دقیق اتصال را ببینید.
*   از مسیر `/test-api` برای تست سلامت API ورکر استفاده کنید.

---

## مدیریت API

**آدرس پایه**: `https://your-domain/UUID/api/preferred-ips`

<div dir="rtl">

| متد | کارکرد | بدنه (JSON) |
| :--- | :--- | :--- |
| `GET` | دریافت لیست فعلی | (خالی) |
| `POST` | افزودن IP جدید | `{"ip": "1.2.3.4", "port": 443, "name": "تست"}` |
| `DELETE` | حذف IP | `{"ip": "1.2.3.4"}` |
| `DELETE` | حذف همه | `{"all": true}` |

</div>

*نکته: ابتدا باید گزینه «اجازه مدیریت API» را در پنل روشن کنید.*

---

## تاریخچه ستاره

[![نمودار تاریخچه ستاره‌های پروژه در گیت‌هاب](https://api.star-history.com/svg?repos=byJoey/cfnew&type=Timeline)](https://www.star-history.com/#byJoey/cfnew&Timeline&LogScale)
