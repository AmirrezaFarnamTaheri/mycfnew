# راهنمای جامع از صفر تا اجرا 🚀

این راهنما شما را مرحله‌به‌مرحله از شروع تا اجرای کامل CFnew همراهی می‌کند و برای هر مرحله روش بررسی ارائه می‌دهد.

---

## فهرست

1. آماده‌سازی
2. ساخت UUID
3. دیپلوی Worker
4. تنظیم متغیرها و KV
5. بررسی آدرس‌ها
6. استفاده از داشبورد
7. اتصال کلاینت‌ها
8. گزینه‌های پیشرفته
9. نکات امنیتی
10. رفع اشکال

---

## 1) آماده‌سازی

### 1.1 حساب Cloudflare
1. به https://dash.cloudflare.com/sign-up بروید.
2. ثبت‌نام کنید و ایمیل را تایید کنید.

### 1.2 موارد موردنیاز
- یک مرورگر مدرن
- UUID (مرحله بعد)
- حدود 10 تا 15 دقیقه زمان

---

## 2) ساخت UUID (کلید محرمانه)

UUID مثل رمز شماست؛ آن را محرمانه نگه دارید.

- **آنلاین:** https://www.uuidgenerator.net/ (نسخه 4)
- **خط فرمان:** دستور `uuidgen`

نمونه (استفاده نکنید):
`84852332-6229-4467-935e-6386566d5823`

---

## 3) دیپلوی Worker

### 3.1 ساخت Worker
1. Cloudflare Dashboard → **Workers & Pages**
2. **Create Application** → **Create Worker**
3. یک نام تصادفی انتخاب کنید
4. **Deploy**

### 3.2 جایگزینی کد
1. روی **Edit Code** کلیک کنید
2. کد پیش‌فرض را حذف کنید
3. محتوای `worker.js` را جایگذاری کنید
4. **Save and Deploy**

---

## 4) تنظیم متغیرها و KV

### 4.1 تنظیم UUID (ضروری)
1. Worker → **Settings** → **Variables and Secrets**
2. متغیر محیطی:
   - نام: `u`
   - مقدار: UUID شما
3. **Deploy**

> توجه: نام متغیر باید **`u`** باشد، نه `uuid`.

### 4.2 اتصال KV (برای داشبورد)
1. Sidebar → **Workers & Pages** → **KV**
2. **Create Namespace** (مثلا `CONFIG`)
3. برگردید به Worker → **Settings** → **Variables and Secrets**
4. **KV Namespace Bindings** → **Add binding**:
   - نام: `C`
   - Namespace: `CONFIG`
5. **Save and Deploy**

---

## 5) بررسی آدرس‌ها (خیلی مهم)

آدرس‌های زیر را در مرورگر باز کنید:

1. **صفحه اصلی**
   - `https://<worker>.workers.dev/`
   - باید ترمینال نمایش داده شود

2. **داشبورد**
   - `https://<worker>.workers.dev/<UUID>`

3. **Region API**
   - `https://<worker>.workers.dev/<UUID>/region`

4. **Subscription**
   - `https://<worker>.workers.dev/<UUID>/sub`

---

## 6) استفاده از داشبورد

- **System Status**: منطقه Worker، روش تشخیص، IP فعلی
- **Config Management**: ذخیره/خواندن/ریست تنظیمات
- **Latency Test**: تست گروهی، فیلتر شهر، افزودن به لیست ترجیحی
- **Debug Console**: نمایش خطاهای JS

### 6.5 توضیح جزئی گزینه‌ها
- **تعیین منطقه (wk)**: منطقه Worker را برای انتخاب مسیر ثابت می‌کند.
- **انتخاب پروتکل**: VLESS/Trojan/xhttp + VMess/SS/TUIC/Hysteria2/gRPC (فقط لینک).
- **تنظیمات ECH**: فعال‌سازی ECH + DoH سفارشی + دامنه سفارشی.
- **مسیر سفارشی (d)**: داشبورد به مسیر مخفی منتقل می‌شود.
- **ProxyIP (p)**: پنهان‌سازی خروجی یا حل Cloudflare loop.
- **IPهای ترجیحی (yx/yxURL)**: ورودی دستی یا دریافت از URL.
- **Latency Test**: تست گروهی، فیلتر شهر، جایگزینی/افزودن به `yx`.
- **کنترل پیشرفته**: API، تطبیق منطقه، downgrade، TLS-only، غیرفعال‌سازی ترجیحی.
- **فیلتر IP/اپراتور**: محدود کردن تحلیل لیست ترجیحی.

---

## 7) اتصال کلاینت‌ها

### 7.1 Android (v2rayNG)
1. نصب v2rayNG
2. افزودن اشتراک
3. قرار دادن لینک `/sub`
4. بروزرسانی و اتصال

### 7.2 iOS (Shadowrocket)
1. افزودن لینک اشتراک
2. بروزرسانی و اتصال

### 7.3 Windows (v2rayN)
1. افزودن لینک اشتراک
2. بروزرسانی و انتخاب نود فعال

### 7.4 macOS / Linux (Clash / sing-box)
- لینک اشتراک را وارد کنید

---

## 8) گزینه‌های پیشرفته

### 8.1 مسیر سفارشی
- `d=/مسیر-مخفی`
- دسترسی با UUID غیرفعال می‌شود

### 8.2 ProxyIP
- برای عبور از محدودیت‌ها
- مثال: `1.2.3.4:443`

### 8.3 فعال‌سازی ECH
- از داشبورد فعال کنید
- سپس اشتراک را بروزرسانی کنید

### 8.4 صفحه اصلی جعلی
- با متغیر `homepage`

### 8.5 SOCKS5
- با متغیر `s=user:pass@host:port`

---

## 9) نکات امنیتی

- UUID را عمومی نکنید
- مسیر سفارشی `d` توصیه می‌شود
- داشبورد را با Cloudflare Access محدود کنید
- در صورت لو رفتن UUID، سریع تغییر دهید

---

## 10) رفع اشکال

**گیر کردن روی Checking...**
- کنسول دیباگ را باز کنید
- `/region` یا `/sub` را بررسی کنید

**UUID نامعتبر**
- نام متغیر `u` باشد
- فاصله نداشته باشد

**KV تنظیم نشده**
- KV بسازید و با `C` متصل کنید
- دوباره دیپلوی کنید

**اشتراک خالی**
- مستقیما `/<UUID>/sub` را باز کنید

---

اکنون باید یک استقرار کامل و قابل استفاده داشته باشید.

مرجع کامل (همه متغیرها، APIها و فرمت‌ها): `docs/REFERENCE_FA.md`.
