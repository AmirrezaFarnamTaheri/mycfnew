# راهنمای جامع صفر تا صد 🚀

این راهنمای جامع شما را از یک مبتدی کامل به مرحله‌ای می‌رساند که یک پروکسی کاملاً کاربردی و مقاوم در برابر فیلترینگ را روی Cloudflare Workers اجرا کنید.

---

## مرحله ۱: آماده‌سازی 🛠️

قبل از نوشتن هر کدی، باید ابزارها را آماده کنیم.

### ۱.۱. حساب کاربری Cloudflare
1.  به [dash.cloudflare.com](https://dash.cloudflare.com/sign-up) بروید.
2.  برای یک حساب رایگان ثبت‌نام کنید.
3.  آدرس ایمیل خود را تأیید کنید.

### ۱.۲. تولید UUID (کلید مخفی شما)
شناسه UUID مانند رمز عبور شما عمل می‌کند. شما به یک شناسه منحصر‌به‌فرد نیاز دارید.
*   **گزینه الف (آنلاین):** به [uuidgenerator.net](https://www.uuidgenerator.net/) بروید و یک UUID نسخه ۴ (Version 4) را کپی کنید.
*   **گزینه ب (خط فرمان):** ترمینال خود را باز کنید و دستور `uuidgen` را اجرا کنید.
*   *نمونه UUID:* `84852332-6229-4467-935e-6386566d5823` (**از این استفاده نکنید!**)

---

## مرحله ۲: استقرار (Deployment) ☁️

ما اسکریپت "Worker" را که به عنوان سرور عمل می‌کند، مستقر خواهیم کرد.

### ۲.۱. ساخت Worker
1.  وارد داشبورد Cloudflare شوید.
2.  در نوار کناری سمت چپ، روی **Workers & Pages** کلیک کنید.
3.  روی دکمه آبی **Create Application** کلیک کنید.
4.  روی **Create Worker** کلیک کنید.
5.  یک نام تصادفی انتخاب کنید (مثلاً `azure-sky-proxy`) تا شناسایی نشوید.
6.  روی **Deploy** کلیک کنید.

### ۲.۲. نصب کد
1.  روی **Edit Code** کلیک کنید.
2.  فایلی به نام `worker.js` خواهید دید. **تمام محتویات داخل آن را پاک کنید.**
3.  کد موجود در فایل `worker.js` این مخزن را کپی کنید.
4.  آن را در ویرایشگر Cloudflare جایگذاری کنید.
5.  روی **Save and Deploy** (بالا سمت راست) کلیک کنید.

### ۲.۳. تنظیم UUID
1.  به صفحه اصلی Worker برگردید (روی فلش برگشت `<` بالا سمت چپ کلیک کنید).
2.  به مسیر **Settings** -> **Variables and Secrets** بروید.
3.  روی **Add variable** در بخش "Environment Variables" کلیک کنید.
    *   **Variable name**: `u`
    *   **Value**: شناسه UUID خود را که در مرحله ۱.۲ ساختید، جایگذاری کنید.
4.  روی **Deploy** کلیک کنید.

### ۲.۴. تنظیم ذخیره‌سازی KV (حیاتی برای داشبورد)
داشبورد برای ذخیره تنظیمات شما به فضایی نیاز دارد.
1.  در نوار کناری اصلی، به **Workers & Pages** -> **KV** بروید.
2.  روی **Create a Namespace** کلیک کنید.
    *   نام: `CONFIG`
    *   روی **Add** کلیک کنید.
3.  به Worker خود برگردید -> **Settings** -> **Variables and Secrets**.
4.  به پایین اسکرول کنید تا به **KV Namespace Bindings** برسید.
5.  روی **Add binding** کلیک کنید.
    *   **Variable name**: `C` (حتماً باید `C` بزرگ باشد).
    *   **KV Namespace**: گزینه `CONFIG` را انتخاب کنید.
6.  روی **Save and Deploy** کلیک کنید.

---

## مرحله ۳: اعتبارسنجی و داشبورد 🖥️

1.  آدرس Worker خود را پیدا کنید (مثلاً `https://azure-sky-proxy.username.workers.dev`).
2.  شناسه UUID خود را به انتهای آدرس اضافه کنید:
    `https://azure-sky-proxy.username.workers.dev/84852332-6229-4467-935e-6386566d5823`
3.  این لینک را در مرورگر خود باز کنید.
4.  باید رابط کاربری **ترمینال با استایل ماتریکس** را ببینید.
5.  باید پیام "Connection Successful!" را نمایش دهد و داشبورد بارگذاری شود.

**عیب‌یابی:**
*   *404 Not Found*: شناسه UUID را به آدرس اضافه نکرده‌اید.
*   *Error 1101*: مشکل در کد. لاگ‌ها را بررسی کنید.
*   *KV Error*: مرحله ۲.۴ را انجام نداده‌اید.

---

## مرحله ۴: تنظیمات کلاینت 📱

حالا دستگاه خود را به Worker متصل می‌کنیم.

### ۴.۱. دریافت لینک اشتراک
1.  در داشبورد، دکمه **[ Quick Subscription ]** یا **[ Copy Link ]** را پیدا کنید.
2.  اگر می‌خواهید از تنظیمات خاصی (مثل "Best Latency") استفاده کنید، ابتدا آن‌ها را در داشبورد تنظیم کنید، سپس لینک را کپی کنید.

### ۴.۲. اندروید (v2rayNG)
1.  برنامه **v2rayNG** را از گوگل پلی یا گیت‌هاب نصب کنید.
2.  برنامه را باز کنید.
3.  منوی همبرگری (☰) را بزنید -> **Subscription Group setting**.
4.  روی `+` (بالا سمت راست) بزنید.
    *   **Remarks**: `Cloudflare Worker`
    *   **Optional URL**: لینک خود را جایگذاری کنید.
5.  ذخیره کنید.
6.  منوی سه نقطه (⋮) را بزنید -> **Update subscription**.
7.  یک نود را انتخاب کنید (مثلاً `VLESS-TLS-443`).
8.  لوگوی V (پایین سمت راست) را بزنید تا متصل شوید.

### ۴.۳. iOS (Shadowrocket / Streisand)
1.  برنامه **Shadowrocket** را باز کنید.
2.  روی `+` بزنید -> Type: **Subscribe**.
3.  **URL**: لینک خود را جایگذاری کنید.
4.  روی **Done** بزنید.
5.  دکمه اتصال را روشن کنید.

### ۴.۴. ویندوز (v2rayN)
1.  برنامه **v2rayN** را دانلود کنید.
2.  **Subscription Group** -> **Add Subscription**.
3.  لینک را جایگذاری کنید.
4.  **Update Subscription**.
5.  روی یک نود کلیک راست کنید -> **Set as active server**.
6.  System Proxy -> **Set System Proxy**.

---

## مرحله ۵: بهینه‌سازی پیشرفته ⚡

### ۵.۱. رفع مشکل "Cloudflare Loop" (ProxyIP)
اگر برخی سایت‌ها باز نمی‌شوند، به ProxyIP نیاز دارید.
1.  به داشبورد بروید -> **Config Management**.
2.  گزینه **ProxyIP (p)** را پیدا کنید.
3.  یک IP یا دامنه تمیز وارد کنید (مثلاً `proxyip.example.com`).
    *   *نکته*: اسکریپت دارای پشتیبان داخلی است، اما IP اختصاصی بهتر است.
4.  روی **Save Config** کلیک کنید.

### ۵.۲. فعال‌سازی ECH (ضد فیلترینگ)
1.  در داشبورد -> **Protocols**.
2.  تیک **Enable ECH** را بزنید.
3.  روی **Save** کلیک کنید.
4.  اشتراک خود را در کلاینت به‌روزرسانی کنید.
    *   *تأثیر*: این کار SNI را رمزگذاری می‌کند و تشخیص اتصال شما به Cloudflare را برای فایروال دشوارتر می‌کند.

---

## واژه‌نامه 📖

*   **UUID**: شناسه کاربر. مانند رمز عبور عمل می‌کند.
*   **VLESS**: یک پروتکل سبک برای پروکسی.
*   **KV**: ذخیره‌سازی کلید-مقدار. پایگاه داده Cloudflare برای ذخیره تنظیمات شما.
*   **Sub/Subscription**: لینکی که تمام جزئیات سرور شما را به صورت خودکار در خود دارد.
*   **ProxyIP**: یک سرور واسط برای مخفی کردن ترافیک شما از دید وب‌سایت مقصد.
