# مثال پستچی: درک نحوه کار پروکسی‌ها 📬

برای درک کامل نحوه کار این ابزار در دور زدن فیلترینگ، بیایید آن را به یک **سیستم پستی امن** تشبیه کنیم.

## نقش‌ها 🎭

*   **شما (Client)**: فرستنده نامه. شما می‌خواهید پیامی به **یوتیوب** بفرستید.
*   **فایروال (سانسورچی)**: بازرس سخت‌گیر در اداره پست محلی که آدرس "گیرنده" روی تمام پاکت‌ها را می‌خواند. اگر ببیند "YouTube"، نامه را می‌سوزاند.
*   **Cloudflare Worker (مرکز پردازش)**: یک مرکز لجستیک بین‌المللی بزرگ و مورد اعتماد.
*   **UUID (تمبر مخفی)**: یک تمبر خاص و نامرئی روی پاکت شما که ثابت می‌کند شما یک مشتری تأیید شده هستید.
*   **ProxyIP (پیک)**: راننده‌ای که بسته را از مرکز پردازش به مقصد نهایی می‌برد.

---

## فرآیند 🔄

### ۱. بسته‌بندی (TLS و رمزگذاری) ✉️
شما نامه‌ای به یوتیوب می‌نویسید. اما نمی‌توانید آن را مستقیماً بفرستید.
*   **رمزگذاری**: ابتدا نامه را با یک کد سری (Cipher) می‌نویسید که فقط مرکز پردازش (Worker) می‌تواند بخواند.
*   **پاکت بیرونی**: نامه را داخل یک پاکت می‌گذارید.
*   **آدرس**: روی پاکت می‌نویسید **"گیرنده: مرکز Cloudflare"**. شما **نمی‌نویسید** "YouTube".
*   **تمبر مخفی (UUID)**: پشت پاکت را با UUID خود مهر می‌زنید.

### ۲. عبور از سانسورچی (ارسال) 👮‍♂️
شما نامه را در صندوق پست می‌اندازید.
*   **بازرس (فایروال)** به پاکت نگاه می‌کند.
*   او آدرس "To: Cloudflare" را می‌بیند. کلودفلر یک کسب‌وکارهای قانونی است که بانک‌ها و فروشگاه‌ها از آن استفاده می‌کنند.
*   او نمی‌تواند داخل پاکت را ببیند (رمزگذاری شده).
*   او اجازه عبور می‌دهد.

### ۳. در مرکز پردازش (Worker) 🏭
نامه به Cloudflare Worker می‌رسد.
*   **تأیید هویت**: Worker **مهر UUID** را بررسی می‌کند.
    *   *مهر اشتباه است؟* نامه خرد می‌شود (قطع اتصال).
    *   *مهر معتبر است؟* Worker پاکت را باز می‌کند.
*   **رمزگشایی**: Worker پیام را رمزگشایی می‌کند و می‌خواند: *"لطفاً این ویدیو را از یوتیوب بگیر."*

### ۴. مایل آخر (مسیریابی) 🚚
حالا Worker باید ویدیو را از یوتیوب بگیرد.
*   **روش الف (مستقیم)**: Worker مستقیماً به یوتیوب می‌رود.
    *   *ریسک*: یوتیوب لباس فرم Worker (آی‌پی کلودفلر) را می‌بیند. برخی سایت‌ها (مثل نتفلیکس) این را دوست ندارند.
*   **روش ب (ProxyIP)**: Worker درخواست را به یک پیک آزاد (**ProxyIP**) می‌دهد.
    *   پیک به یوتیوب می‌رود. یوتیوب یک شخص عادی (آی‌پی مسکونی) را می‌بیند.
    *   پیک ویدیو را می‌گیرد و به Worker برمی‌گرداند.

### ۵. سفر بازگشت ↩️
Worker نوار ویدیو را در یک پاکت جدید می‌گذارد، می‌نویسد "فرستنده: Cloudflare" و آن را برای شما می‌فرستد. بازرس یک بسته از طرف Cloudflare می‌بیند و اجازه عبور می‌دهد.

---

## تکنیک‌های پیشرفته فرار 🥷

### Encrypted Client Hello (ECH) - "پاکت دو لایه"
معمولاً، حتی با TLS، آدرس "گیرنده" (SNI) در اولین دست‌دادن قابل مشاهده است.
*   **بدون ECH**: شما بلند می‌گویید "سلام کلودفلر!". بازرس می‌شنود.
*   **با ECH**: شما *کل* پاکت را داخل یک جعبه بزرگ‌تر و مات می‌گذارید. جعبه **هیچ** برچسبی جز "به: ابر (Cloud)" ندارد.
*   بازرس حتی نمی‌داند با کدام *بخش* از کلودفلر صحبت می‌کنید. این لبه تکنولوژی حریم خصوصی است.

### Fragmentation - "خردکن"
به جای ارسال نامه به صورت یک تکه، آن را به ۱۰ نوار باریک برش می‌دهید و یکی‌یکی پست می‌کنید.
*   بازرس نواری با نوشته "You..." می‌بیند. نادیده می‌گیرد.
*   او "...Tube" را می‌بیند. نادیده می‌گیرد.
*   Worker تمام نوارها را دریافت می‌کند، به هم می‌چسباند و "YouTube" را می‌خواند.

### Hysteria / TUIC (UDP) - "دسته پهپادها"
*   **TCP (VLESS)**: مثل کامیون. در جاده می‌راند، پشت چراغ قرمز می‌ایستد و برای تحویل امضا می‌گیرد. مطمئن است اما ممکن است کند باشد یا در ایست بازرسی گیر کند.
*   **UDP (Hysteria)**: مثل دسته‌ای از پهپادها. آن‌ها از بالای ترافیک پرواز می‌کنند، منتظر امضا نمی‌مانند و اگر یکی سقوط کند، بقیه به راه خود ادامه می‌دهند. بسیار سریع‌تر برای بازی و شبکه‌های ناپایدار.
